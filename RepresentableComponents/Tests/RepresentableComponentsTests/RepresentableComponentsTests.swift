    import XCTest
    @testable import RepresentableComponents

    final class RepresentableComponentsTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(RepresentableComponents().text, "Hello, World!")
        }
    }
